<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/backgroundColor"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/films_details_layout_container"
        android:name="com.example.krendikova.presentation.films.FilmsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible">
        <include
            android:id="@+id/films_details_layout"
            layout="@layout/film_details_layout" />
    </FrameLayout>

    <FrameLayout
        android:id="@+id/error_layout_container"
        android:name="com.example.krendikova.presentation.films.FilmsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">
        <include
            android:id="@+id/error_layout"
            layout="@layout/error_layout" />
    </FrameLayout>

</LinearLayout>